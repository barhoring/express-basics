extends layout.pug

block content
  section#content
    h3= text
      
    if hint
      p
        i Hint: #{hint}
 
    a(href=`${id}?side=${sideToShow}`)= sideToShow
    br
    a(href='/cards') hit me!